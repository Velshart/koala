<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Article | Koala</title>
    <link rel="stylesheet"
          href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
          integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
          crossorigin="anonymous"
    >
</head>
<body>

<div class="container">
    <h1>{{article.title}}</h1>

    <div class="btn-group gap-2" role="group">
        <form action="/home" method="get">
            <button type="submit" class="btn btn-primary">Exit</button>
        </form><br>

        <form action="/article-chapters/new/{{article.id}}" method="get">
            <button type="submit" class="btn btn-primary">New chapter</button>
        </form><br>

        <form action="/articles/update/{{article.id}}" method="get">
            <input type="hidden" name="createdAt" value="{{article.createdAt}}">
            <button type="submit" class="btn btn-primary">Change title</button>
        </form><br>
    </div>

    <h3>Chapters</h3>

    <div class="row row-cols-1 g-3">

        {{#articleChapters}}
            <div class="col">

                <div class="card shadow-sm">

                    <div class="card-body">
                        <h5 class="card-title">{{title}}</h5>
                        <p class="card-text"><small class="text-muted">Created at: {{createdAt}}</small></p>

                        <div class="d-flex gap-2">
                            <form action="/article-chapters/view/{{id}}" method="get">
                                <input type="hidden" name="articleId" value="{{article.id}}">
                                <button type="submit" class="btn btn-outline-primary btn-sm">View</button>
                            </form>

                            <form action="/article-chapters/delete/{{id}}" method="post">
                                <input type="hidden" name="articleId" value="{{article.id}}">
                                <button type="submit" class="btn btn-outline-danger btn-sm">Delete</button>
                            </form>
                        </div>
                    </div>

                </div>

            </div>
        {{/articleChapters}}

        {{^articleChapters}}
            <p>This article has no chapters.</p>
        {{/articleChapters}}
    </div>
</div>
</body>
</html>